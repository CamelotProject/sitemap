imports:
    - { resource: services.yml }

framework:
    test: true
    session: { storage_id: session.storage.mock_file }
    secret: Hell yeah!
    router: { resource: "%kernel.project_dir%/src/Bridge/Symfony/Resources/config/routing.yml" }

camelot_sitemap:
    base_host: http://localhost
